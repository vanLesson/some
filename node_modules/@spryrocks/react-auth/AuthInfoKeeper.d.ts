import IAuthInfoKeeper, { AuthInfoKeeperDelegate } from './IAuthInfoKeeper';
import Session from './Session';
import { IStorageBackend } from '@react-native-community/async-storage';
export default class AuthInfoKeeper implements IAuthInfoKeeper {
    private readonly delegate;
    private readonly storage;
    constructor(delegate: AuthInfoKeeperDelegate, storage: IStorageBackend);
    isAuthenticated(): Promise<boolean>;
    authenticate: (resp: Session) => Promise<void>;
    update: (resp: Session) => Promise<void>;
    getAuthInfo: () => Promise<Session | undefined>;
    reset: () => Promise<void>;
    private syncApiToken;
    initialize(): Promise<void>;
}

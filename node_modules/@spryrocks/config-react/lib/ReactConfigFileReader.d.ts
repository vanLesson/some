import { IConfigFileReader, Config } from '@spryrocks/config-core';
export declare type Resources = {
    [key: string]: Config | undefined;
};
export declare class ReactConfigFileReader implements IConfigFileReader {
    private readonly resources;
    constructor(resources: Resources);
    read(file: string): Config;
}

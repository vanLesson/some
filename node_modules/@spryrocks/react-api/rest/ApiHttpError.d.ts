import ApiError from './ApiError';
export default class ApiHttpError extends ApiError {
    readonly status: number;
    readonly statusText: string;
    readonly data: any;
    readonly cause: any;
    constructor(status: number, statusText: string, data: any, cause: any);
}

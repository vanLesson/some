export interface AuthInfo {
    accessToken: string;
    refreshToken: string;
}
export default interface ApiDelegate {
    getHeaders: () => any;
    getAuthInfo: () => Promise<AuthInfo | undefined>;
    updateAuthInfo: (authInfo: AuthInfo) => Promise<void>;
}
